"""Skills category — discoverable instruction/reference bundles.

Each subfolder is a self-contained skill whose `SKILL.md` YAML frontmatter
(`name` + `description`) acts as its manifest. Discovered by `registry.list_skills`
and loaded by `scripts/get-skill.py`. See README.md for the contract.
"""
